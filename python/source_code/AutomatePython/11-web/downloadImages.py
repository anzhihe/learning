import requests
import bs4
from selenium import webdriver

# open browser
browser = webdriver.Firefox()
# open page
browser.get(url)

# get query box and send keys: selenium
# submit: selenium
# get images links: bs4
# download images by request and save 
